name: 007 Operations V2 Bootstrap Lock

on:
  push:
    branches: [007-operations-v2]
    paths:
      - 'operations-v2/package.json'
      - 'operations-v2/tsconfig.json'
      - 'operations-v2/.gitignore'
      - '.github/workflows/v2-bootstrap-lock.yml'

permissions:
  contents: write

concurrency:
  group: operations-v2-bootstrap-lock
  cancel-in-progress: true

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    defaults:
      run:
        working-directory: operations-v2

    steps:
      - name: Checkout V2 branch
        uses: actions/checkout@v4
        with:
          ref: 007-operations-v2
          fetch-depth: 1

      - name: Set up Node 24
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Resolve deterministic dependency graph
        shell: bash
        run: |
          set -euo pipefail
          npm config set package-lock true
          npm install --package-lock-only --ignore-scripts --no-audit --no-fund
          test -s package-lock.json

      - name: Commit generated lockfile
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "$(git status --porcelain -- package-lock.json)" ]]; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add package-lock.json
            git commit -m 'chore(v2): lock bootstrap dependencies'
            git push origin HEAD:007-operations-v2
          else
            echo 'package-lock.json already up to date'
          fi

      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund

      - name: Typecheck and build
        id: build
        shell: bash
        run: |
          set +e
          npm run build 2>&1 | tee /tmp/v2-build.log
          code=${PIPESTATUS[0]}
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Persist build diagnostic
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "commit=$GITHUB_SHA"
            echo "exit_code=${{ steps.build.outputs.exit_code }}"
            echo "---"
            tail -n 120 /tmp/v2-build.log
          } > .bootstrap-diagnostic
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .bootstrap-diagnostic
          if ! git diff --cached --quiet; then
            git commit -m 'ci(v2): persist bootstrap diagnostic'
            git push origin HEAD:007-operations-v2
          fi

      - name: Enforce successful build
        shell: bash
        run: |
          test "${{ steps.build.outputs.exit_code }}" = "0"

      - name: Record validated bootstrap commit
        shell: bash
        run: |
          set -euo pipefail
          printf '%s\n' "$GITHUB_SHA" > .bootstrap-green
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .bootstrap-green
          if git diff --cached --quiet; then
            echo 'bootstrap marker already current'
            exit 0
          fi
          git commit -m 'ci(v2): record successful bootstrap validation'
          git push origin HEAD:007-operations-v2
